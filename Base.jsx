
import { Stack, Typography, Box } from '@mui/material'
import React from 'react'

const Base = ({children}) => {
  return (
        <Box p={'1rem'} mt={'4.4rem'}>
        {children}
        </Box> 
  )
}

export default Base;
